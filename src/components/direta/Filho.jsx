import React, { useState } from 'react'
import { propTypes } from 'react-bootstrap/esm/Image'

export default props => {
    return(
        <>
               <h2>{props.a}</h2>
               <h2>{props.b}</h2>
        </>
    )
}